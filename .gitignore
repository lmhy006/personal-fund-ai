# ===================== Python 通用 =====================
__pycache__/
*.pyc
*.pyo
*.pyd
*.pyw
.Python
env/
venv/
.venv/
ENV/
env.bak/
venv.bak/
*.egg-info/
*.egg
dist/
build/
*.whl
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
# Phase 4 薄工具层/前向运行期测试的临时 fixture（tests/ 创建）
ml/_agt_test_*/
ml/_fwd_test_*/

# ===================== akshare下载缓存 / 临时文件 =====================
*.tmp
*.temp
*.log
logs/
cache/

# ===================== VS Code / IDE =====================
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# ===================== 系统文件 =====================
.DS_Store
Thumbs.db
desktop.ini


data/
*.pdf

# Phase 2B 临时实验输出；完整逐月汇总与预测明细单独跟踪
ml/experiments/*smoke*_monthly.csv
ml/experiments/gbdt_v2[abcd]_monthly.csv
ml/experiments/gbdt_v2align_[abcd]_monthly.csv
ml/experiments/preds_gbdt_v2align_[abcd]_monthly.csv

# ===================== Phase 3 中间产物（可再生成，勿提交） =====================
# 滚动风格/行业暴露缓存（每套 10~28MB，共约 100MB；跑 style_momentum_check.py 可重建）
ml/backtest/*.parquet
# panel v3 复核的逐基金预测留存（每份 32MB；用 model_ridge.py --panel ml/panel_v3.parquet 可重建）
ml/experiments/preds_ridge_v3panel_*_monthly.csv
# 注：data/ 已整体忽略 → 净值缓存（data/raw/fund_nav/）与清洗视图不再跟踪；
#     但清盘基金代码台账、失败清单、EID 查询参数、数据哈希用 git add -f 强制纳入（体积小、不易复原）
# 注：ml/panel_v3.parquet（35.9MB）与 ml/attrs/fund_attrs_monthly.parquet 未忽略——它们是研究基线数据
#     （与已提交的 ml/panel.parquet 同类），如需瘦身可另行忽略并在 README 标注重建命令
